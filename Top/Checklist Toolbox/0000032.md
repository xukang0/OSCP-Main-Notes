[[2375 Docker API]]

```
docker images
```

```
docker run -v /:/mnt --rm -it <IMAGE_NAME> chroot /mnt sh
```

Alternatively : 

```
docker run -v /:/mnt --rm <IMAGE_NAME> chroot /mnt sh
```

```
docker run -v /:/mnt --rm <IMAGE_NAME> /bin/sh -c "chroot /mnt sh"
```