Feroxbuster Directories Brute Force (Medium)
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");
const discoveredDomain = page?.["Discovered Web Domain"] ?? "NO DOMAIN FOUND";

const command = `feroxbuster -u http://${discoveredDomain}/ -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt -t 42 --filter-status 400,401,403,404`;

dv.paragraph("```bash\n" + command + "\n```");
```
