#### Proving Grounds Practice
 
 Box Setup
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const machine = page?.MACHINE ?? "NO MACHINE FOUND";

const command = `cd ~/Desktop/PEN200/ && mkdir ${machine} && cd ${machine}`;

dv.paragraph("```bash\n" + command + "\n```");
```

Folder setup
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const machine = page?.MACHINE ?? "NO MACHINE FOUND";

const command = `cd ~/Desktop/PEN200/${machine} && touch Users.txt && touch Passwords.txt && mkdir nmap && cd nmap`;

dv.paragraph("```bash\n" + command + "\n```");
```
VPN
```
cd ~/Desktop/ovpn/ && sudo openvpn PEN200.ovpn
```
