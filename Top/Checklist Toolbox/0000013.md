Feroxbuster FIle Directories Sweep (Medium)
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");
const discoveredDomain = page?.["Discovered Web Domain"] ?? "NO DOMAIN FOUND";

const command = `feroxbuster -u http://${discoveredDomain}/ -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt -x php,html,txt,bak,zip -t 42 -d 2`;

dv.paragraph("```bash\n" + command + "\n```");
```