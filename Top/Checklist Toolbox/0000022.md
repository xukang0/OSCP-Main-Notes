#### HackTheBox
 
 Box Setup
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const machine = page?.MACHINE ?? "NO MACHINE FOUND";

const command = `cd ~/Desktop/HTB/ && mkdir ${machine} && cd ${machine}`;

dv.paragraph("```bash\n" + command + "\n```");
```

NMAP folder setup
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const machine = page?.MACHINE ?? "NO MACHINE FOUND";

const command = `cd ~/Desktop/HTB/${machine} touch Users.txt && touch Passwords.txt && mkdir nmap && cd nmap`;

dv.paragraph("```bash\n" + command + "\n```");
```
VPN
```
cd ~/Desktop/ovpn/ && sudo openvpn htb.ovpn
```
