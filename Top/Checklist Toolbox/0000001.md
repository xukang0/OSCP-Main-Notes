Full TCP Scan
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const ip = page?.IP ?? "NO IP FOUND";

const command = `rustscan -a ${ip} --ulimit 5000 -- -sC -sV -Pn`;

dv.paragraph("```bash\n" + command + "\n```");
```
UDP Scan
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const ip = page?.IP ?? "NO IP FOUND";

const command = `sudo nmap -sU -sV --top-ports 20 --max-retries 1 -Pn -oA nmap/udp_scan ${ip}`;

dv.paragraph("```bash\n" + command + "\n```");
```