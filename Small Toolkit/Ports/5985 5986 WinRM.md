[[NetExec]]
[[Cracking/Password Cracker/CrackMapExec|CrackMapExec]]
[[Synced OSCP Notes/Small Toolkit/Tools/Evil-Winrm|Evil-Winrm]]

Login WinRM
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const ip = page?.IP ?? "NO IP FOUND";

const command = `nxc winrm ${ip} -u [USER] -p [PASS]`;

dv.paragraph("```bash\n" + command + "\n```");
```
Interact and run cmd
```dataviewjs
const page = dv.page("Synced OSCP Notes/Top/Active Machine");const ip = page?.IP ?? "NO IP FOUND";

const command = `nxc winrm ${ip} -u [USER] -p [PASS] -x 'whoami /priv'`;

dv.paragraph("```bash\n" + command + "\n```");
```